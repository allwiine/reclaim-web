{
  "name": "reclaim-site",
  "version": "1.0.0",
  "description": "Marketing and support site for Reclaim, the macOS developer-storage cleaner.",
  "type": "module",
  "private": true,
  "engines": {
    "node": "^22.22.0 || >=24.8.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "html-validate \"dist/**/*.html\""
  },
  "dependencies": {
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "html-validate": "^11.7.0",
    "wrangler": "^4.123.0"
  }
}
